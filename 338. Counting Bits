/**
 * @param {number} n
 * @return {number[]}
 */
var countBits = function(n) {
    let bin=0,arr=[]
    for(i=0;i<=n;i++){
     bin = i.toString(2).split('').filter(num => num == 1).length
    arr[i]=bin
    }
    return arr
};